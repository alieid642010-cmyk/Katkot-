# كتكوت برو — البنية المصدرية (المرحلة 0)

## إيه اللي اتغيّر
الكود بقى منظّم فى ملفات مصدر منفصلة بدل سكربت واحد بحجم 1.29 مليون حرف، مع سكربت بناء (`build.js`) يجمّعهم فى ملف HTML واحد للنشر — **بنفس الشكل بالظبط** اللي التطبيق كان عليه (Single-file، Offline-first، جاهز لـ GitHub Pages/PWABuilder).

**التحقق:** أول بناء بعد التقسيم طلع **مطابق بايت-لبايت 100%** للملف الأصلي (`cmp` رجّع صفر فروق) — يعني التقسيم نفسه مفيهوش أي مخاطرة، هو تنظيم بحت.

## البنية
```
src/
  templates/          → الهيكل الثابت (head, body shell, tail) كملفات HTML منفصلة
  styles/main.css      → كل الـ CSS (كان جوه <style> واحد ضخم)
  boot/                → سكربت منع "ومضة" الوضع الليلي (بيشتغل قبل أي رسم)
  app-logic/            → 28 ملف JS منظّم حسب الوظيفة (بدل سكربت واحد 660+ دالة)
    manifest.json       → ترتيب دمج الملفات (لازم يفضل بنفس الترتيب — فيه اعتماديات بين الملفات)
build.js               → node build.js  →  ينتج dist/index.html (الملف النهائي للنشر)
check-permissions.js   → node check-permissions.js  →  فحص آلي يمنع رجوع ثغرة الصلاحيات
```

## أقسام app-logic/ الـ28
| # | الملف | المحتوى |
|---|---|---|
| 00 | reference-data-and-benchmarks | الكتالوج المرجعي، معايير السلالات، تقييم أداء العمال |
| 01 | state-storage | الحالة والتخزين، Firebase Sync |
| 02 | auth-roles | التوثيق، الصلاحيات، إذن الاستخدام |
| 03 | modals | النوافذ المنبثقة العامة |
| 04 | heating-estimation | تقدير التدفئة + صور السجل اليومي |
| 05 | feed-formulation-calculator | حاسبات العلف والمحاليل |
| 06 | batch-crud | عمليات الدفعات |
| 07 | inventory-helpers | مساعدات المخزون |
| 08 | daily-record-crud | السجل اليومي |
| 09 | purchases-crud | المشتريات |
| 10 | sales-crud | المبيعات |
| 11 | custom-items-crud | الحركات المالية الحرة |
| 12 | operations-biosecurity-crud | العمليات والسلامة الحيوية |
| 13 | stock-manual-movement | حركة المخزون اليدوية |
| 14 | reminders-vaccines-treatments-additives | التذكيرات/اللقاحات/العلاجات/البروتوكولات |
| 15 | core-production-calculations | الحسابات الإنتاجية الجوهرية |
| 16 | feasibility-study | دراسة الجدوى |
| 17 | alerts-engine | محرك التنبيهات |
| 18 | render-router | موجّه العرض الرئيسي |
| 19 | dashboard | لوحة التحكم |
| 20 | daily-tab | تبويب اليومي |
| 21 | inventory-purchases-sales | المخزون والمشتريات والمبيعات (واجهة) |
| 22 | operations-biosecurity-tab | تبويب العمليات والسلامة |
| 23 | finance-tab | تبويب المالية |
| 24 | compare-tab | تبويب المقارنة |
| 25 | charts | الرسوم البيانية Canvas2D |
| 26 | export-pdf-print | التصدير والطباعة |
| 27 | dark-mode-toggle | تبديل الوضع الليلي |

## 🏗️ المرحلة 1 من خطة الهندسة — تقدّم حتى الآن
1. **نقطة تعديل مركزية للـ state:** `setState(key, value)` فى `01-state-storage.js` — كل الـ32 نقطة اللي كانت بتعدّل `state.X` مباشرة (فى 8 ملفات) بقت تعدّي من هنا. سجل تشخيصي مؤقت (`_stateChangeLog`, آخر 50 تعديل) متاح للـConsole وقت الدعم الفني.
2. **تخطي render() الكامل للتفاعلات عالية التكرار:** `toggleChecklistTask()` (تيك بند تشيك ليست فى السجل اليومي) بقى بينادي `renderDailyWorkerBox()` بس بدل `render()` الكامل. النمط ده آمن لأنه اتطبّق بس على دوال ليها **مصدر نداء واحد فعلي** (اتأكد بـ grep قبل التغيير) — أي دالة زي `toggleVaccine`/`toggleTreatment`/`toggleReminder` ليها أكتر من نقطة نداء (تبويب الإنتاج + السجل اليومي) فضلت على `render()` الكامل عمدًا لحد ما نعمل نفس الفحص عليها بعناية.
   - **مقايضة موثّقة:** شارة عدد التنبيهات فى شريط التبويبات وبانر التنبيهات العلوي مش بيتحدّثوا فورًا لما تشيك ليست يغيّر تنبيه مرتبط بيه — بيترجعوا يتزامنوا تلقائيًا عند أي `render()` كامل تاني (تبديل تبويب، أي حفظ تانى). أثر بصري بسيط جدًا ومؤقت، مقابل توفير هدم/بناء الصفحة كاملة على كل تيك.
3. **المتبقي:** باقي الدوال اللي بتنادي `render()` كامل من داخل صناديق فرعية (زي `dailyApplyAdditive`) محتاجة نفس فحص "مصدر نداء واحد" قبل أي تحويل — مش اتعمل لسه، عشان الأمان.

## 🏗️ المرحلة 1 — الجولة التانية (تدقيق شامل على الـ113 نداء `render()` المتبقيين)
راجعت كل دالة بجسمها بتنادي `render()` كامل (113 دالة، بعد استبعاد الـ`toggleChecklistTask` اللي اتصلحت الجولة اللي فاتت) وقسمتهم:

1. **`dailyApplyAdditive` / `applyAdditiveToday`** — اتراجعت زي ما كان متسجل. **مش مرشحة**: بتتنادى فعليًا من مصدرين مختلفين (تبويب السجل اليومي *و* تبويب المخزون مباشرة)، فمفيش "مصدر نداء واحد" آمن. اتقفلت كخطوة متبقية — الملاحظة فى الكود التوثيقي (`21-inventory-purchases-sales.js`) كافية، مفيش داعي لتغيير.

2. **6 دوال subtab/selection ظاهريًا شبيهة بـ`toggleChecklistTask`** (`setDailyEventsSubTab`, `setManagementSubTab`, `setSettingsSubTab`, `toggleCompare`, `setExpansionSpecies`, + `toggleAccessGrantPanel`) — بعد فحص التنفيذ الفعلي لكل واحدة، طلع **5 منهم مش نفس الحالة فعليًا**: الفرق الجوهري إن `toggleChecklistTask` كانت بتغيّر حاجة صغيرة جوه تبويب كبير (يهد التبويب كله عشان تيك واحد)، لكن الـ5 دول (subtab switches + اختيار نوع/دفعة للمقارنة) *أصلاً* بيغيّروا **كل** محتوى القسم المعروض — يعني إعادة الرسم مش هدر، هي بالظبط المطلوب حصوله. تطبيق نفس النمط عليهم كان هيضيف تعقيد (container جديد + دالة رندر جزئي لكل واحدة) من غير أي فايدة أداء حقيقية، وبيزوّد خطر نسيان side-effect (زي إعادة حساب `alerts`/رسم الشارتس) كانت بتحصل تلقائيًا جوه `render()`. **قرار: اتسابوا زي ما هم — full render() صحيح معماريًا هنا.**

3. **`toggleAccessGrantPanel`** — دي الوحيدة اللي طلعت مطابقة فعليًا لحالة `toggleChecklistTask` (بانل صغير بيتوسّع جوه قائمة طلبات الدخول، هي نفسها جوه تبويب إعدادات كبير فيه كذا قسم تاني مش له علاقة). **اتنفّذت:**
   - استُخرجت قائمة طلبات الدخول لدالة مستقلة `renderAccessRequestsListCard()` (فى `02-auth-roles.js`، جنب باقي منطق الصلاحيات)
   - اتحطت جوه حاوية `<div id="accessReqListBox">` بدل ما كانت مبنية inline جوه `renderSettingsTab()`
   - دالة جديدة `refreshAccessRequestsListBox()` بتحدّث الصندوق ده بس، مع fallback آمن لـ`render()` الكامل لو الصندوق مش موجود لأي سبب
   - `toggleAccessGrantPanel()` وأزرار اختيار مستوى الصلاحية (كامل/جزئي) جوه البانل نفسه بقوا يستخدموا الدالة الجديدة بدل `render()` المباشر
   - **تحقق:** `node --check` نظيف، `check-permissions.js` نظيف (100% — 82 نقطة زي ما كانت)، الوظيفة (فتح/قفل بانل، تبديل مستوى الصلاحية، سماح/رفض/إلغاء) كلها بتعدّي بنفس المسار القديم (`decideAccessRequest`, `revokeAccess`, إلخ) من غير أي تغيير — بس مسار العرض بقى أضيق.

## ✅ المرحلة 1 — إغلاق نهائي (كل خصائص state الجذرية بقت تعدّي من setState، بدون استثناء)
كان فاضل 39 نقطة (فى 6 ملفات) بتعدّل خاصية جذرية فى `state` مباشرة (`state.X = ...`) من غير ما تعدّي من `setState()`. راجعتهم واحدة واحدة وحوّلتهم كلهم:

| الملف | عدد النقاط | النوع الغالب |
|---|---|---|
| `02-auth-roles.js` | 27 | كتلة `loadState()` كاملة (تهيئة/ترحيل أول تحميل) + تصحيح `activeTab` بعد تحديث الصلاحيات |
| `06-batch-crud.js` | 1 | تبديل `activeId` بعد تطبيق بروتوكول من نافذة تعديل الدفعة |
| `27-export-pdf-print.js` | 8 | استرجاع من السلة (`GLOBAL_TRASH_RESTORE`)، استيراد باكب، تصدير/طباعة (`appSettings`)، `farmName`، `trash`، `activeId` |
| `00-reference-data-and-benchmarks.js` | 1 | تهيئة `speciesOverrides` قبل حفظ تعديل مرجعي |
| `12-operations-biosecurity-crud.js` | 1 | تهيئة `conflictRules` |
| `14-reminders-vaccines-treatments-additives.js` | 3 | تهيئة `protocols` (3 دوال بروتوكولات مختلفة) |
| `18-render-router.js` | 5 | منطق دمج/إلغاء التبويبات القديمة داخل `renderInner()` |
| `21-inventory-purchases-sales.js` | 1 | `setExpansionSpecies` |
| `24-finance-tab.js` | 1 | تهيئة `appSettings` |
| `26-charts.js` | 1 | تسجيل تاريخ آخر نسخة احتياطية |

**استُثنيت عمدًا (برّه نطاق `setState` بتصميمه، مش نسيان):** كل نقطة `state = {...}` اللي بتستبدل **الكائن كله** مش خاصية جذرية واحدة — تحميل من السحاب (`01-state-storage.js`)، استعادة نسخة محلية (`02-auth-roles.js`)، استيراد باكب (`27-export-pdf-print.js`). دي عملية مختلفة جوهريًا (استبدال المرجع نفسه، مش تعديل مفتاح جواه)، فمفيش داعي تعدّي من دالة مصممة أصلاً لخاصية واحدة.

**التحقق النهائي:**
- `grep` شامل على الـ28 ملف بعد التعديل: **صفر** نقاط `state.X = ...` متبقية (غير الاستثناءات الموثقة فوق والتعليقات التوضيحية).
- `node --check` نظيف 100%.
- `check-permissions.js` نظيف 100% (82 نقطة زي ما كانت — التحويل لـ`setState` ملوش أي علاقة بمنطق الصلاحيات، بس تغيير مسار الكتابة).
- الملف اتبنى (`node build.js`) من غير أي خطأ، والحجم زاد بفارق طبيعي (~250 حرف) نتيجة استبدال `X = Y` بـ`setState('X', Y)` نصيًا، من غير أي تغيير سلوك.

**النتيجة: المرحلة 1 (طبقة الحالة) مقفولة بالكامل — مفيش نقطة تعديل state جذرية واحدة متسربة برّه `setState()`، ومفيش دالة `render()` كامل بتتنادى من مكان بديل أرخص كان ممكن يتعمل من غيرها. الجاهز دلوقتي: المرحلة 2 (مكوّنات واجهة قابلة لإعادة الاستخدام).**


```bash
node build.js               # يبني dist/index.html من src/
node check-permissions.js   # يفحص أي دالة save/delete/add/remove جديدة ناقصة حماية
```
اعمل التعديل فى ملف `src/app-logic/` المناسب (مش فى ملف HTML واحد ضخم بعد كده)، وبعدين `node build.js` ينتجلك الملف النهائي.

---

## 🔒 نتائج فحص الصلاحيات — الجولة الثانية (بعد بناء الأداة الآلية)
بعد ما اتبنت `check-permissions.js`، طلعت 42 دالة `save*/delete*/add*/remove*/update*` من غير حماية ظاهرة. راجعتهم يدويًا بمقارنة كل دالة بشقيقتها (لو فيه `deleteX` محمية بالفعل بـ owner، فـ `saveX` المقابلة المفروض تتحمي بنفس المستوى):

### ✅ اتصلحت فورًا (10 دوال — نفس نمط الثغرة المؤكد بمقارنة الشقيقة المحمية):
| الدالة | scope | بمقارنة |
|---|---|---|
| `deleteRecord(date)` | `production` | يتماشى مع `saveDaily` |
| `saveVaccine()` | `owner` | `deleteVaccine` محمية owner |
| `saveTreatment()` | `owner` | `deleteTreatment` محمية owner |
| `saveProtocol()` | `owner` | `deleteProtocol` محمية owner |
| `saveFeedAdditive()` | `owner` | `deleteAdditive` محمية owner |
| `saveWaterAdditive()` | `owner` | `deleteAdditive` محمية owner |
| `saveDoseModal()` | `owner` | `deleteAdditive` محمية owner |
| `saveIncident()` | `production` | يتماشى مع نمط "التسجيل اليومي" |
| `deleteIncident(id)` | `owner` | يتماشى مع `deleteBiosecurity` |
| `saveOutageLog()` | `owner` | `deleteOutageLog` محمية owner |

**عدد نقاط `requirePermission()` الآن: 60** (كانت 43 قبل أي مراجعة، 50 بعد الجولة الأولى).

### ⏳ محتاجة مراجعتك الشخصية (32 دالة متبقية)
دول معظمهم **على الأرجح آمنين** لكن محتاجين قرارك لأنهم مش زي النمط الواضح اللي فوق (مفيش دالة حذف شقيقة أقارن بيها بثقة). قسمتهم حسب احتمالية الخطورة:

**غالبًا محتاجة حماية (راجعها الأول):**
- `saveFarmsList`, `removeFarmFromList*` — إدارة قائمة المزارع (لو عامل قدر يمسح مزرعة كاملة من القائمة = خطر حقيقي)
- `addChecklistTask/removeChecklistTask`, `addConflictRule/removeConflictRule`, `addFeedTransition/*`, `addTransitionDay/*` — إعدادات بروتوكولات (فى `operations-biosecurity-crud.js`)
- `saveExpansionInputs` — مدخلات خطة التوسع (قرار استراتيجي)
- `saveQuickIntervention/deleteQuickIntervention` — تدخلات سريعة

**غالبًا آمنة (تفضيلات شخصية/واجهة لا تؤثر على بيانات مشتركة):**
- `saveDashboardOrder`, `updateOnboardingStepUI`, `saveManualLatLon`, `saveFarmName`, `updateMarketPrice*` — إعدادات عرض أو بيانات مرجعية عامة مش حساسة لنفس درجة الخطورة

**دوال تخزين داخلية (على الأغلب آمنة لأنها بتتنادى من دوال محمية أصلًا — تحتاج تأكيد بس مش تصليح):**
- `saveAuth`, `saveToStorage` — تم التأكد يدويًا إن `saveAuth` دالة تخزين منخفضة المستوى بينادوها من دوال محمية بالفعل

**التوصية:** راجع القائمة دي فى جلسة قادمة (المرحلة 3 من الخطة المعمارية) — الأداة (`check-permissions.js`) هتفضل تكشفلك أي دالة جديدة تضيفها ناقصة الحماية تلقائيًا من غير ما تحتاج تتذكر كل ده يدويًا.

---

## 🔒 الجولة الثالثة — إقفال الملف بالكامل
راجعت الـ32 دالة المتبقية واحدة واحدة بقراءة الكود الفعلي (مش تخمين):

### ✅ اتصلحت (22 دالة إضافية):
| الدالة | scope | السبب |
|---|---|---|
| `saveStdRefs` | `owner` | بتعدّل المنحنى المرجعي المستخدم فى كل التحليلات |
| `saveAdvancedSettings` | `owner` | إعدادات عامة (تدفئة/تنبيهات/أداء/جدوى) لكل المزرعة |
| `removeItemPhoto` | `management` | تعديل بيانات صنف مخزون مشترك |
| `addChecklistTask`/`removeChecklistTask` | `management` | قالب بروتوكول السلامة الحيوية |
| `addConflictRule`/`removeConflictRule` | `management` | قواعد تعارض الأصناف |
| `addFeedTransition`/`removeFeedTransition` | `management` | جدول تحويل العلف |
| `addTransitionDay`/`removeTransitionDay`/`updateTransitionDayPct` | `management` | تفاصيل جدول التحويل |
| `updateMarketPrice`/`updateMarketPriceFromDashboard` | `production` | تسجيل يومي روتيني (مطابق لـ saveDaily) |
| `saveFeedStageForBatch` | `production` | بيانات إنتاجية يومية |
| `saveExpansionInputs` | `management` | خطة توسع استراتيجية |
| `saveManualLatLon` | `management` | موقع المزرعة |
| `saveQuickIntervention` | `production` | تدخل سريع (زي saveDaily) |
| `deleteQuickIntervention` | `owner` | حذف — نفس نمط كل عمليات الحذف |
| `saveContact` | `management` | جهات اتصال المزرعة |
| `saveFarmName` | `management` | اسم المزرعة المعروض |
| `saveDashboardOrder` | `management` | إعداد مشترك (Firestore) يؤثر على شكل اللوحة للجميع |

### ✅ اتأكد إنها آمنة (10 دوال — مُضافة لـ ALLOWLIST مع السبب موثّق فى الكود):
- `updateStdRefCell`, `addStdRefRow`, `removeStdRefRow` — بتعدّل مسودة محلية قبل الحفظ، البوابة الحقيقية `saveStdRefs()` بقت محمية
- `saveFarmsList`, `removeFarmFromListUI`, `removeFarmFromList` — قائمة أجهزة محلية فقط، بيانات المزرعة نفسها آمنة على السحابة دائمًا
- `saveAuth`, `saveToStorage` — دوال تخزين داخلية بتتنادى من دوال محمية بالفعل
- `removeDailyPhoto` — مسودة محلية قبل حفظ السجل اليومي
- `updateOnboardingStepUI` — واجهة بحتة، مفيش تعديل بيانات

**النتيجة النهائية: 82 نقطة `requirePermission()`** (كانت 43 فى البداية). أداة `check-permissions.js` رجّعت **نظيفة 100%** — مفيش دالة تعديل واحدة فى كل الـ28 ملف من غير حماية ظاهرة أو سبب موثّق لعدم الحاجة ليها.

---

## 🧪 المرحلة 3 — طبقة منطق قابلة للاختبار

### اكتشاف مهم قبل أي شغل
فحصت آليًا كل الـ71 دالة `compute*` (محرك التحليلات والحسابات) للتأكد هل هي "نقية" فعلًا (بترجع بيانات بس، من غير ما تلمس الشاشة أو التخزين مباشرة) — ده شرط أساسي عشان تقدر تُختبر آليًا بدون متصفح.

**النتيجة: 70 من 71 دالة (98.6%) نقية بالفعل.** يعني اتفاقية التسمية `compute*` = حساب نقي، `render*` = عرض على الشاشة كانت متبعة بانضباط طول الوقت من غير ما تكون موثّقة رسميًا. الدالة الوحيدة غير النقية (`computeWaterDilution`) هي حاسبة تخفيف دواء فى الماء بتكتب النتيجة مباشرة فى صندوق نتيجة على الشاشة — حالة خاصة (حاسبة تفاعلية فورية)، مش خلل معماري.

### الأداة المبنية: بيئة اختبار Node بدون متصفح
- **`tests/test-env.js`** — يحمّل ملف التطبيق الكامل (المبني من `build.js`) جوه بيئة Node معزولة (باستخدام `vm` الأساسية فى Node نفسه)، بمحاكاة مبسّطة لـ`document`/`localStorage`/المؤقّتات، عشان تقدر تنادي أي دالة `compute*` مباشرة وتتأكد من رقمها الصحيح — من غير ما تفتح متصفح ولا تدخل بيانات يدويًا.
- **`tests/ventilation.test.js`** (22 اختبار) — يغطي `computeMinVentTarget`، `getNightVentDutyRow`، `computeVentilationPlan` (بما فيها التحسين الجديد لربط الأمونيا بنسبة التشغيل)، `fmt`، `seasonLabelOf`.
- **`tests/permissions.test.js`** (33 اختبار) — يغطي `requirePermission()` نفسها فى كل السيناريوهات (مالك، عامل بلا صلاحيات، عامل بصلاحية جزئية، عامل بكل الصلاحيات ما عدا owner) + تأكيد وجود الـ21 دالة الأخطر من الـ82 المحمية.
- **`tests/run-all.js`** — يشغّل كل ملفات `*.test.js` مرة واحدة ويرجّع ملخص شامل.

### كيف تستخدمها
```bash
node tests/run-all.js          # يشغّل كل الاختبارات (55 اختبار حاليًا)
node tests/ventilation.test.js # ملف واحد بس
```
أي مرة تضيف دالة `compute*` جديدة أو تعدّل واحدة موجودة، تقدر تضيف اختبار جديد فى ملف `.test.js` مناسب (أو تعمل ملف جديد) بنفس النمط — الأداة (`test-env.js`) جاهزة تحمّل أي دالة موجودة فى التطبيق مباشرة.

### حالة المرحلة 3
| البند | الحالة |
|---|---|
| فصل الحسابات كدوال نقية عن الـDOM | ✅ (كانت متحققة بالفعل بنسبة 98.6% — اتأكدت آليًا بس) |
| فحص آلي: كل دالة save/delete عندها requirePermission | ✅ (`check-permissions.js` — من المرحلة السابقة) |
| بيئة اختبار Node بدون متصفح | ✅ جديد |
| اختبارات فعلية (55 اختبار) | ✅ جديد |

---

## 🧩 المرحلة 2 — مكوّنات واجهة قابلة لإعادة الاستخدام (إقفال كامل)
اتعمل الشغل ده فى فرع مختلف بالتوازي مع المرحلة 3، واتدمج هنا بعد كده — نفس الملف `07-inventory-helpers.js`:

### 1) `renderTableOrEmpty(hasData, headersHtml, rowsHtml, emptyIcon, emptyText, tbodyId)`
استُخرج من نمط `<div class="card scroll-x">${hasData?`<table>...`:`<div class="empty">...`}</div>` المتكرر. اتطبّق على **7 جداول** فى `21-inventory-purchases-sales.js` (أرصدة المخزن، سجل الحركة، المشتريات، مقارنة الموردين، المبيعات، مقارنة المشترين).

**باقي الجداول (40 إجمالي فى الكود) اتسابت عمدًا:** 18 منها جداول طباعة/تصدير PDF (`26-charts.js` + قسم الطباعة فى `24-finance-tab.js`) بتصميم مختلف تمامًا (border/cellpadding مباشرة لتوافق الطباعة)، والباقي كل واحد فيه خصائص شكلية/تفاعلية مختلفة (عمود إضافي، صف إجمالي...) بتخليه مش نفس القالب — تطبيق الدالة عليهم كان هيزود تعقيد بدل ما يقلله.

### 2) `statLine(k, v, opts)`
استُخرج من نمط `<div class="stat-line"><span class="k">...</span><span class="v">...</span></div>` — **أكتر نمط متكرر فى الكود كله**. `opts` بتاخد `lineStyle`/`lineId`/`kStyle`/`vStyle`/`vId` اختياريًا لتغطية كل التنويعات الشكلية.

**النتيجة النهائية: 157 من 157 حالة اتحولت (100%).** التحويل اتعمل على مرحلتين:
- **109 حالة** بخصائص شكلية ثابتة نصيًا — تحويل آلي بسكريبت تحليل نصي (regex متوازن) + اختبار مباشر للدالة بمدخلات حقيقية للتأكد من تطابق الـHTML الناتج حرفيًا.
- **48 حالة** بتعبيرات JS ديناميكية جوه الـstyle نفسه (زي `style="color:${x?'a':'b'}"`) — راجعتها وحولتها **يدويًا واحدة واحدة** (مش آليًا) عشان تفادي أي خطأ escaping فى التعبيرات المتداخلة، مع بناء واختبار (`node --check` + `check-permissions.js` + `tests/run-all.js` — 55 اختبار) بعد كل ملف على حدة قبل ما أكمل التالي.
- **حالة واحدة استُبعدت عمدًا** (فى `16-feasibility-study.js`، كارت "التوصية" اللي بيستخدم كلاس `stat-line` بس مفيهوش `span.v` أصلاً — تصميم مختلف مش نفس النمط، تحويلها كان غلط مش تبسيط).

**التحقق النهائي:** `node --check` نظيف، `check-permissions.js` نظيف (82 نقطة زي ما كانت)، `tests/run-all.js` نظيف (55/55 اختبار ناجح)، وعدّ نهائي بـgrep أكد **صفر** حالة `stat-line` خام متبقية فى كل الـ28 ملف (غير الاستثناء الموثّق فوق).

**النتيجة: المرحلة 2 مقفولة بالكامل.**

## 🩺 الحالة الظاهرية اليومية + التنبؤ الاستباقي بالمرض (ميزة جديدة، مش من خطة الهندسة)
بناءً على طلب مباشر: إضافة فحص سريري بصري (checklist أعراض) للسجل اليومي، يتغذّى منه محرك استدلال يتوقع
احتمال مرض قبل ما ينتشر، بالاستفادة من قاعدة معرفة الحوادث الموجودة أصلاً (`computeIncidentKnowledgeBase`).

- **`CLINICAL_SIGN_GROUPS`** (`00-reference-data-and-benchmarks.js`): 6 مجموعات أعراض (تنفسي، هضمي/زرق، حركة/أرجل،
  عصبي، سلوك عام، ريش/جلد) — كل واحدة بـ2-4 أعراض بكود ثابت. مصدر واحد يُستخدم فى بناء الفورم وفى المطابقة.
- **`CLINICAL_DISEASE_KB`**: مرجع بيطري عام (6 حالات: كوكسيديا، جمبورو، كوليباسيلوس، CRD، نيوكاسل، إجهاد حراري)
  — نطاق عمري + أعراض أساسية/مساندة + توصية عملية. استرشادي فقط، دايمًا بيوصي بمراجعة طبيب بيطري.
- **`computeSymptomPrediction(b, currentSigns)`** (`15-core-production-calculations.js`، دالة نقية بلا DOM):
  بتطابق الأعراض مع المرجع العام + قاعدة معرفة حوادث المزرعة نفسها (بونص ثقة + ملاحظة "حصل عندك قبل كده" لو
  فيه نمط شخصي قريب من نفس العمر) + بونص استمرارية لو نفس العرض ظاهر من "أمبارح" كمان. مُختبرة بـ8 اختبارات
  آلية (`tests/symptom-prediction.test.js`) تغطي: مفيش أعراض، تطابق داخل/خارج النطاق العمري، تعدد أعراض أساسية،
  بونص الاستمرارية، ونقاء الدالة (مفيش أثر جانبي على كائن الدفعة).
- **الفورم**: قسم جديد فى `essentialsBox` (مرة واحدة لليوم، مش نهار/ليل) — checkboxes مبنية ديناميكيًا من
  `CLINICAL_SIGN_GROUPS`، + معاينة حية للتوقع بمجرد التأشير (`updateClinicalPredictionPreview`، من غير ما يحتاج حفظ).
- **بعد الحفظ**: لو فيه توقع "قوي" (`urgent: true`)، بيظهر `showInfo` مُلفِت حتى بعد ما المودال يقفل.
- **سجل الأيام السابقة**: أيقونة 🩺 جنب عمود الصحة لو اليوم فيه أعراض مسجّلة، وتفاصيل كاملة (أعراض + توقعات)
  فى نافذة تفاصيل اليوم (`showRecordDetail`).
- **التحقق:** `node --check` نظيف، `check-permissions.js` نظيف (82 نقطة)، `tests/run-all.js` نظيف (63/63 اختبار).

### تحديث: وزن الذاكرة الشخصية متدرّج حقيقي (n/(n+K)) بدل بونص ثابت
كان فيه بونص ثابت (+1) لو فيه نمط شخصي قريب — اتصلح ليبقى بنفس منطق `FARM_CURVE_SHRINKAGE_K` المستخدم فعليًا
لمنحنى نمو المزرعة، بس بثابت انكماش أصغر (`DISEASE_MEMORY_SHRINKAGE_K = 4`) لأن حالات صحية مؤكدة نادرة نسبيًا:
- `n` = عدد الحوادث **المؤكدة بتشخيص فعلي** (`diseaseTag` مسجَّل يدويًا، مش شذوذ تلقائي) بنفس التشخيص تقريبًا
  وفى عمر قريب (±6 أيام)، عبر `mineAllIncidentRecords` (تتطلب دورتين مؤرشفتين على الأقل من نفس النوع أصلًا).
- وزن الذاكرة الشخصية = `n/(n+4)` — عند n=0 اعتماد 100% على `CLINICAL_DISEASE_KB` (المرجع البيطري العام)،
  وكل ما زادت الحالات المؤكدة، الوزن يرتفع تدريجيًا (n=1 → 20%، n=4 → 50%، وهكذا لحد ما يقترب من 100% بدون ما يوصله).
- الدرجة النهائية `blendedScore` = مزيج مرجّح بين درجة الأعراض حسب المرجع العام ودرجة أعلى ثابتة (تعكس ثقة أكبر)
  لو فيه تاريخ شخصي مؤكد، مش مجرد جمع. الملاحظة المعروضة للمستخدم بتوضح النسبة صراحة + الحل اللي نفع قبل كده.
- **8 اختبارات إضافية** (16 إجمالي فى الملف ده) بتغطي: n=0 (اعتماد كامل على المرجع العام)، n=1 (20% بالظبط)،
  n=4 (50% بالظبط)، ارتفاع الثقة مع زيادة n، وعزل التشخيصات المختلفة عن بعض (تشخيص لمرض تانى ميأثرش على تقييم مرض غيره).

### تحديث: صورة للحالة + تسجيل صوت التنفس
مرفقين اختياريين جوّه قسم الحالة الظاهرية نفسه (منفصلين عن "صور توثيقية" العامة لليوم، عشان يفضلوا مربوطين
بالفحص السريري تحديدًا وقت المراجعة لاحقًا):
- **📷 صورة للحالة** (حتى صورتين) — نفس آلية الضغط المشتركة (`compressImageFile`, 800px, جودة 0.6).
- **🎙️ تسجيل صوت التنفس** (حتى 15 ثانية) — عبر `MediaRecorder`/`getUserMedia`، بيتوقف تلقائيًا عند الحد الأقصى،
  بيتخزن base64 (`audio/webm` أو `audio/mp4` حسب دعم المتصفح)، مع رسالة واضحة لو الجهاز/المتصفح مش بيدعم التسجيل
  أو لو إذن الميكروفون اتمنع. التسجيل بيتوقف تلقائيًا لو المودال اتقفل من غير حفظ.
- بيتخزنوا جوّه `r.clinicalSigns.photos` / `r.clinicalSigns.respiratoryAudio`، وبيظهروا فى معاينة الفورم قبل الحفظ
  وفى نافذة "تفاصيل اليوم" بعد الحفظ. وجود صورة أو تسجيل صوتي وحده (من غير ما تتأشّر أي أعراض) كافي لاعتبار
  اليوم "فيه حالة ظاهرية مسجّلة" (أيقونة 🩺 فى الجدول).
- **ملحوظة:** التسجيل الصوتي مُستخدم للتوثيق والمراجعة فقط (تسمعه بنفسك أو تشاركه مع الطبيب البيطري) — مفيش
  تحليل آلي لمحتوى الصوت فى النسخة دي.

### تحديث: دليل الأمراض بقى قابل للتعديل الكامل + تحسينات بصرية من مقارنة مع تطبيق آخر
بمقارنة مع تطبيق مزرعة تانى (لقطات شاشة قدّمها أحمد)، لوحظ إن `DISEASE_LIBRARY` (دليل أمراض قديم، ثابت فى الكود،
9 أمراض) و`CLINICAL_DISEASE_KB` (الجديدة، 6 أمراض) كانا نظامين منفصلين ومتكررين جزئيًا. اتوحّدوا فى نظام واحد:
- **`state.diseaseKB`**: القائمة بقت جزء من الـstate نفسه (زي `checklistTemplate`) — قابلة للإضافة/التعديل/الحذف
  بالكامل من المستخدم عبر شاشة "🩺 دليل الأمراض" (زر ➕ لإضافة مرض، ضغطة على أي مرض للتعديل، ✕ أحمر للحذف).
  الترحيل (`getDefaultDiseaseKB()`) بيحصل مرة واحدة عند أول تحميل، بنفس نمط ترحيل المرحلة 1، ويدمج كل الـ9 أمراض
  القديمة مع الـ6 الجديدة (مع إزالة التكرار) = **9 أمراض فريدة** فى القائمة الافتراضية.
- **كل مرض له**: اسم، نطاق عمري، وصف أعراض حر، ملاحظة تفريق عن أمراض تانية، توصية، **وربط اختياري بأعراض
  الفحص السريري اليومي** (أساسي/مساند لكل عرض من `CLINICAL_SIGN_GROUPS`) — لو حدّد المستخدم عرض أساسي واحد
  على الأقل، المرض يشارك فى `computeSymptomPrediction`؛ لو سابها فاضية (زي التسمم بالأفلاتوكسين أو النقرس، اللي
  مالهومش توقيع أعراض واضح يتفحص بصريًا)، المرض يفضل للتصفح/المرجعية بس ومش هيظهر كتوقع نشط أبدًا.
- **`computeSymptomPrediction`** بقت تقرأ من `state.diseaseKB` لو موجودة، وترجع للقائمة الافتراضية (`getDefaultDiseaseKB()`)
  فى بيئة اختبار معزولة أو لو الترحيل لسه ما حصلش — نفس التوافق الخلفي المستخدم فى أماكن تانية بالتطبيق.
- **3 اختبارات إضافية** (19 إجمالي فى الملف): مرض مخصّص مضاف يدويًا بيظهر فى التوقعات، وجود `state.diseaseKB`
  يوقف استخدام القائمة الافتراضية القديمة تلقائيًا معاها، ومرض بدون `requiredSigns` ميظهرش أبدًا كتوقع نشط.
- **تحسينات بصرية عامة** (مش خاصة بالأمراض بس — تنطبق على كل نوافذ التطبيق): خط سحب علوي (`.modal::before`)
  على كل الـbottom sheets زي أغلب تطبيقات الموبايل، وكلاس `.del-x` (دائرة حمراء بعلامة ✕) لعناصر القوائم القابلة
  للحذف، مستخدم فى دليل الأمراض ومتاح لأي قائمة قابلة للتعديل تتضاف مستقبلًا (جهات الاتصال مثلًا).

### تحديث: أدوار جهات الاتصال بقت قابلة للتعديل الكامل
نفس نمط دليل الأمراض بالظبط، بس لأدوار جهات الاتصال (مورد/طبيب بيطري/فني صيانة/عامل/أخرى):
- **`state.contactRoles`**: قائمة `{id, label, icon}` قابلة للإضافة/التعديل/الحذف من شاشة "🏷️ إدارة أدوار جهات
  الاتصال" (زر 🏷️ جنب "إضافة جهة اتصال سريعة")، بنفس نمط `.del-x` المستخدم فى دليل الأمراض.
- الترحيل (`getDefaultContactRoles()`) بيحصل مرة واحدة عند أول تحميل بـ5 أدوار افتراضية (نفس الأربعة القديمة + "أخرى").
- الـ`<select>` بتاع اختيار الدور فى فورم إضافة جهة اتصال بقى **يتبني ديناميكيًا** من `state.contactRoles` بدل
  خيارات ثابتة فى القالب، وأيقونة كل جهة اتصال فى القائمة (`contactRoleIcon()`) بتتقرأ من نفس القائمة.
- **⚠️ مقايضة موثّقة عمدًا:** شرائح "اتصال سريع" (`renderQuickContactChips`) اللي بتظهر جوّه فورم السجل اليومي
  لسه بتفلتر بالاسم الحرفي `'مورد'` و`'طبيب بيطري'` تحديدًا (مش عبر ID). لو المستخدم غيّر اسمي الدورين دول
  تحديدًا من شاشة الإدارة، الشرائح هتفضل موجودة بس هتوقف تلاقي جهات اتصال تطابقها — تدهور بسيط وسهل الملاحظة
  (مفيش شرائح تظهر)، مش عطل أو بيانات غلط. اتقبلت المقايضة دي عمدًا لتفادي إعادة هيكلة أعمق (ID بدل نص) لميزة
  UI ثانوية، بعكس فئات المشتريات اللي المفروض متتلمسش خالص لأنها مرتبطة بحسابات FCR/مالية مباشرة.

### تحديث: مكوّن فلتر تاريخ موحّد قابل لإعادة الاستخدام
**ملحوظة صراحة:** الكود مكانش فيه فلتر تاريخ متكرر أصلًا يستاهل "توحيد" — كتكوت برو معندوش الميزة دي خالص
قبل كده (على الأغلب لأن كل دفعة محدودة بـ35-45 يوم، فالإلحاح أقل من تطبيق بيوض مستمر شهور/سنين). الشغل ده
بناء جديد من الصفر، اتطبّق أول مرة على سجل التدقيق العام كأوضح مكان محتاج فلترة:
- **`DATE_RANGE_PRESETS`** (`02-auth-roles.js`، جنب باقي الأدوات المشتركة زي `esc`/`fmt`): 9 اختيارات قياسية
  (اليوم/أمس/هذا الشهر/الشهر الماضي/آخر 3 أشهر/آخر 6 أشهر/هذا العام/العام الماضي/كل الوقت) — نفس ترتيب الصور.
- **`dateRangeFromPreset(id)`**: دالة نقية بترجّع `{start, end}` كـ`Date`، أو `null` صراحة لـ`'all_time'`.
- **`isDateInPreset(dateStr, id)`**: بترجّع `false` بأمان لتاريخ فاضي/`null`/`undefined` بدل ما ترمي استثناء.
- **`openDateRangeFilter(currentId, onSelect)`**: بيفتح bottom sheet (نفس نمط الصور بالظبط) عبر `openGenericModal`
  الموجودة أصلًا، والاختيار الحالي متمّيز بلون مختلف عن الباقي.
- **أول تطبيق فعلي**: سجل التدقيق العام (⚙️ إعدادات) — زر `📅 [الفترة الحالية]` جنب عنوان القسم، بيفلتر
  القائمة قبل ما تتقص لآخر 30 عملية. الفلتر ده مؤقت (متغيّر session، مش محفوظ فى الـstate) عمدًا — تفضيل
  عرض بسيط، مش إعداد دائم.
- **13 اختبار جديد** (`tests/date-range-filter.test.js`) بيغطوا: النهاردة/أمبارح، `all_time` بيشمل تواريخ قديمة
  جدًا، تاريخ فاضي بيرجع `false` بأمان بدل استثناء، حدود "آخر 3 مقابل 6 أشهر"، و`dateRangePresetLabel` بترجع
  تسمية آمنة افتراضية لأي id مش معروف.
- **الخطوة الجاية:** المكوّن جاهز يتطبّق على أي شاشة تانية محتاجة فلترة تاريخ (مثلًا قائمة المشتريات/المبيعات
  لو طالت مع الوقت عبر دورات كتير) بنداء واحد بس — مش محتاج بناء من جديد.

### تحديث: البحث الشامل بقى يشمل الميزات والأدوات مش البيانات بس (بديل "مركز القيادة")
بدل بناء شاشة "مركز قيادة" منفصلة (زي تطبيق تانى قدّمه أحمد كمرجع)، اتقرر دمج نفس الفكرة (القفز المباشر لأي
ميزة بالبحث) جوّه `runGlobalSearch` الموجودة أصلًا وشغالة كويس على البيانات — أوفر وأقل تكرار من شاشة موازية:
- **`getFeatureDirectory()`** (`27-export-pdf-print.js`): قائمة ~16 ميزة/أداة قابلة للقفز إليها مباشرة (حاسبة
  العلف، حاسبة محلول الإماهة، دليل المصطلحات، دليل الأمراض، إدارة أدوار جهات الاتصال، أرشيف الدورات، طباعة
  ورقة تعليمات، استيراد CSV، تسجيل انقطاع كهرباء، إضافة دفعة/تحصين/تذكير/بروتوكول، إلخ).
- فئة بحث جديدة "🧭 ميزات وأدوات" ظهرت **أول فئة** فى نتائج البحث الشامل — لو كتبت "حاسبة" أو "دليل" أو "أرشيف"
  مثلًا، بتلاقي الميزة المطلوبة تفتح مباشرة، مش بس نتائج بيانات.
- الضغط على نتيجة ميزة بينقّل التبويب المناسب أولًا، وبعدها بـ150ms بيفتح المودال/الشاشة تلقائيًا (تأخير بسيط
  عشان يضمن إن عناصر DOM بتاعة التبويب الجديد خلصت رندر قبل ما الميزة تحاول تستخدمها).

### تحديث: تقويم شهري مرئي للتنبيهات (بديل بصري اختياري للقائمة الفلات)
آخر بند من الخطة الملهمة من التطبيق التانى — أكبرها حجمًا. اتضاف كـ**عرض بديل اختياري** جنب القائمة الفلات
الموجودة (مش استبدال لها، تقليلًا لمخاطر كسر أي حاجة بتعتمد على القائمة القديمة):
- **زر تبديل** (📅 تقويم / 📋 قائمة) فوق قسم "🔔 تنبيهات مخصصة" — الاختيار session فقط (مش محفوظ فى الـstate).
- **`renderReminderCalendar(b)`**: شهر كامل (grid 7 أعمدة، الأحد على اليمين زي التقويم العربي فى الصور، متوافق
  تلقائيًا مع RTL الأساسي للتطبيق) — كل يوم فيه تنبيهات بيظهر نقط صغيرة تحته (رمادية = منفّذ، حمراء = متأخر،
  ذهبية = قادم). الضغط على يوم بيعرض تفاصيل تنبيهاته تحت التقويم مباشرة (تيك/تعديل/حذف زي القائمة العادية).
- **تنقّل بين الشهور** (‹ ›) وتحديث جزئي فقط (نفس نمط `refreshReminderListBox` الآمن اللي اتفحص فى المرحلة 1 —
  مصدر نداء واحد لكل دالة تحديث).
- كل منطق بناء الأعمدة/الأسابيع مبني بـ`new Date()` القياسية بلا مكتبات خارجية، ومتوافق مع أي شهر/سنة.

## ✅ مراجعة شاملة للتنبيهات (شكل + قابلية تنفيذ + إشعار فوري) — إقفال كامل
كان فيه فرعين شغل متوازيين على نفس الموضوع (تدقيق أمني + اختبارات فى فرع، ومراجعة التنبيهات فى فرع تانى) —
اتدمجوا هنا مع بعض. المنطق نفسه (68 نقطة تنبيه) اتراجع ووُجد سليم فعليًا (عتبات مبنية على معايير إنتاج
حقيقية) — اللي كان ناقص فعلًا هو التصنيف والربط والإشعار، مش المنطق:

1. **`categorizeAlert(text)`** (`17-alerts-engine.js`) — تصنيف تلقائي لكل تنبيه من الـ61 نوع لمجال واحد من
   الأربعة المستخدمين أصلًا فى `computeUnifiedPriorities` (إنتاج/مالية/تشغيل/بيئة، بدل اختراع تصنيف مواز)
   + تحديد التبويب/التبويب الفرعي اللي يحل المشكلة، بناءً على الأيقونة المميزة فى بداية كل نص التنبيه (مع
   تفرقة بالكلمات المفتاحية للحالات المشتركة زي 📊/📦/🔥ال لي بتظهر فى أكتر من سياق).
2. **`computeUnifiedPriorities`** (`19-dashboard.js`) — بدل ما كانت بتلقط بس 3 أنماط نصية محددة من الـ61،
   بقت بتاخد **كل** تنبيهات الخطر/التحذير غير المكتومة بمجالها المصنَّف من `categorizeAlert` تلقائيًا.
3. **`goToAlertAction(actionTab, actionSubTab)`** (`06-batch-crud.js`) — أي تنبيه (فى البانر، القائمة الكاملة،
   كارت "أهم 3 أفعال") بقى نصه نفسه قابل للضغط ويوديك لمكان الحل مباشرة، بدل ما تدور فى التبويبات بنفسك.
4. **إشعار فوري للتنبيهات الخطرة الجديدة** (`notifyNewDangerAlerts` فى `02-auth-roles.js`) — بقى بيستخدم نفس
   نظام `showKatkotNotification` الموجود أصلًا (مش نسخة موازية جديدة، بعد ما اكتشفنا واتأكدنا إن النظام
   الموجود بالفعل متطور جدًا: إشعارات بالوقت المحدد بالظبط لمواعيد الإضافات/المعاملات، فحص دوري كل 45 دقيقة،
   periodic background sync، صوت بديل، Service Worker حقيقي بملف منفصل). الجديد بس: كل تنبيه خطر **جديد
   فعليًا** (مش القديم اللي لسه موجود) بيطلع إشعار فوري بمجرد ظهوره، مقارنة بتوقيع التنبيهات السابق لتفادي
   الإزعاج بالتكرار.

**التحقق النهائي:** `node --check` نظيف، `check-permissions.js` نظيف (82 نقطة)، `tests/run-all.js` نظيف (55/55)،
وتأكيد بـgrep إن مفيش أي دالة معرّفة مرتين (كانت فيه محاولة أولى بالغلط لبناء نظام إشعارات موازي كامل — اتشالت
كلها لما اتأكد إن الموجود أصلًا أرقى، ودمجنا بيه بدل ما نستبدله).

**النتيجة: مراجعة التنبيهات مقفولة بالكامل. الجاي حسب الأولوية المتفق عليها: سقايات المياه المتعددة + حساب
الهادر، بعدين ترحيل زيادة العلف، بعدين تصنيف المخزون بشارتس، بعدين تحديث الشارتس العام، وآخرها الداشبورد
(هيتضاف فيه: قوائم مخصصة قابلة للتعديل للمصروف/الدخل/الدواء، تصدير منفصل لكل تقرير PDF/Excel، شفافية صيغة
الحساب جوه كل مؤشر — "مركز القيادة" و"التقويم المرئي" اتعملوا بالفعل بطريقة مختلفة (بحث شامل موسّع + تقويم
التذكيرات) فمش هيتكرروا).
